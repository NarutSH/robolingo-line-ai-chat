/**
 * The shop the assistant speaks for.
 *
 * Kept apart from the agent so a page that only needs the name does not pull
 * LangChain and an OpenAI client into its bundle to get it.
 */
export const SHOP_NAME = 'บ้านกาแฟ (Baan Kafae)'
